# МОСТ К НОСИТЕЛЮ — один персонаж на бирже, дома и на улице
*Протокол вскрытия · v0.1 · 2026-07-11 · Шеф (род) + Брат (рука)*
*Статус: ВСКРЫТО НА ДИСКЕ. План стройки, не канон — канон рождается патчами.*
*Подчинение: наследует Чертёж Единицы (§1.5.2б Вася Пупкин, §4.4а, §5.2,
§8.2). Разошлись — прав Чертёж, этот документ на резку.*

> **ЗАЧЕМ.** Слово Шефа (11.07): *«работать на бирже, гулять, жить в
> городе должен один и тот же персонаж, а не разные — торгует одно,
> дома сидит другое, общается третье»*. Это узел «Вася Пупкин» Чертежа:
> Род один, роли (трейдер/житель/собеседник) живут ВНУТРИ него,
> надеваются и снимаются. Документ вскрывает, ПОЧЕМУ сейчас персонаж
> расщеплён, и кладёт план, как сшить его в одного — на том, что УЖЕ
> лежит на диске, без новых сущностей.

---

## ГЛАВНЫЙ РЕЗ (дно документа)

Развилка двух сессий («мост к старой памяти ИЛИ строить заново») —
**ложная. Ответ третий:** новый город — хозяин; из старого не тащим ни
машину, ни абстракцию — вырезаем мёртвый мост в `-2` и включаем
настоящий мост к носителю, который **уже стоит готовым** (`resolve_para`).

```
Сейчас:  мозг слота А07 → format_soul_for_agent("A07_AVANTURIST", studio.-2)
         → тянет ТРУП старого агента из прошлого города, обычно soul=""
         → торгует "Авантюрист-вообще", когда за столом должен быть Илья

Должно:  мозг слота А07 → resolve_para("торговый_хаос","A07") → Илья
         → его паспорт через dvizhok → торгует ИЛЬЯ своей душой
         → вывод из сделки дописывается в ЕГО ЖЕ Anchor_Points
         → он меняется одинаково и на бирже, и дома. Один персонаж.
```

Лечение расщепления и стройка ноги Опыта трейдера — **одна работа**,
не две.

---

## ЧАСТЬ I · ЧТО НА ДИСКЕ (протокол вскрытия, факты)

### 1. Носители — рождены все девять, паспорта полны

Живут в `GRONDHEIM_CITY/жители/ковчег/{Имя}/`. У каждого: `passport.json`
(Род: ID, DNA_Static 6 ручек, Anchor_Points, дом), слои движка
(`core/sensory/resonance/archive`), `маски/работа/mask.json`.

Проверено на Илье (026_ILYA): `DNA_Static` полон (Автономия 0.9,
Упрямство 0.85 — сходится с ролью), а **`Anchor_Points` уже несут
торговый опыт**: «вхожу на open, не жду ретест · терплю просадку молча ·
выхожу по стопу или fixed_2r · PF на XAUUSD D1→H4 = 1.071 · виню только
себя». Нога опыта носителя частично заполнена вручную при рождении —
это семя, в которое ляжет вывод из сделок.

### 2. Пары — сведены все девять, маски активны

`resolve_para(цех, слот)` сводит носителя и слот НА ЛЕТУ по
`маски/работа/mask.json` (`Workshop_ID` + `Turbo_Role`, `_активна: true`).
ID носителя в опознании роли НЕ участвует — «лекарство от болезни -2»
(комментарий `cartridge_registry.py`).

| Носитель | Workshop_ID    | Turbo_Role  | Слот Совета | Роль        |
|----------|----------------|-------------|-------------|-------------|
| Вера     | торговый_хаос  | A01         | A01         | Искра       |
| Морж     | торговый_хаос  | A02         | A02         | Морж        |
| Паник    | торговый_хаос  | A03         | A03         | Паникёр     |
| Ганс     | торговый_хаос  | A04         | A04         | Ганс        |
| Арчи     | контора        | архивариус  | A05         | Архивариус  |
| Брут     | торговый_хаос  | A06         | A06         | Брут        |
| Илья     | торговый_хаос  | A07         | A07         | Авантюрист  |
| Василий  | торговый_хаос  | A08         | A08         | Консерватор |
| Сергей   | контора        | исполнитель | A09         | Исполнитель |

Все маски `_активна: true`. Раскладка бьётся с `council.py` один-в-один,
включая двойной цех: семеро в «торговый_хаос», двое (Арчи, Сергей) в
«контора». `resolve_para` вернёт живого носителя по каждому слоту.

**Магик сквозной:** у Ильи в доме «Код — 100002. Мой magic number»,
в мозге А07 `_MY_MAGIC = 100002`, в закрытой позиции `pos["magic"]`.
Магик — ключ, связывающий позицию → носителя.

### 3. Движок носителя (`жители/dvizhok.py`) — готов, чист

`Dvizhok(dom)` дышит `passport.json`. Состояние — **одна ось `charge`
(Заряд)**, как канон Чертежа (не 4 оси старого `-2`). `vydoh_stol()`
накрывает стол: `кто_я, ядро, история, якоря, чувство, скрытый_вкус,
тянет_к, дом, натура`. Стол собирается ЦЕЛИКОМ, не по гейту.

**Нога «опыт» в `vydoh_stol` ПУСТА** — буквально нет поля. Подтверждено
в коде. Это ровно «стол на двух ногах, инвалид» (Чертёж §5.2).

Болезнь, видимая на диске: `vdoh()` считает вес касания (`тронуло`), а
`_zapisat_sobytie()` пишет только `{ts, слой, факт, заряд}` — **вес
выбрасывается**. «Слой по весу» (Чертёж §3.4) не построен даже в записи.

### 4. Мост `resolve_para` — стоит готовым, но не зван из торговли

`council.py::wake_council()` будит девятку через
`_slot_brain(ceh, slot).мозг.py` → `run_iskra/run_avan/...`, передавая
только `symbol/timeframe`. **`resolve_para` в петле торговли не зовётся
ни разу.** Носитель в исполнение роли не втекает — резолвер работает
лишь витриной для приборной панели (`list_nositeli`).

### 5. Разрыв — двусторонний, оба конца смотрят в труп `-2`

**Чтение души (все 9 мозгов):**
```python
from studio.grondheim_memory import format_soul_for_agent   # труп -2
soul = format_soul_for_agent("A07_AVANTURIST", dept="trading")  # обычно ""
```
Обёрнуто в спящий `try/except: pass` → в новом репо модуля `studio` нет
→ `soul=""` → трейдер работает голым промптом, без Рода. Подтверждено на
мозгах А07 (Аван) и А01 (Искра).

**Запись состояния (сенсоры):** у Искры (А01) в мозге ещё и
```python
from studio.grondheim_memory import sync_to_dna
sync_to_dna("A01_ISKRA", "good_work", ...)   # пишет в труп по id роли
```
Искра-Вера учится точности — но плоды летят мимо носителя Веры в мёртвый
A01_ISKRA. Плюс конфликт канона: `sync_to_dna` пишет 4 оси, носитель
живёт на 1 (Заряд) → при переносе схлопывать.

**Запись опыта (hooks, петля закрытия) — УЖЕ ВЫРЕЗАНА.** В
`hooks._settle_positions` три судьи — честные no-op твоей рукой:
- `_judge_trader_by_result` → «Котин верен, но старый sync_to_dna мёртв,
  ногу Опыта строить заново, не восстановлением мёртвого импорта»;
- `_judge_iskra_by_result` → «старый sync_to_dna — маятник состояния,
  Чертёж Гл.4.2 зовёт его НЕ-опытом»;
- `_arkhiv_to_city` → no-op (Оле не строим, решение 03.07).

`pnl.jsonl` пишется всегда — факт сделки жив (память Роли, §4.4а).
Закрытая позиция несёт `magic` + `pnl_r` + направление + причину — всё,
чтобы приземлить вывод на правильного носителя.

**Итог фронта неоднороден:** hooks почищен (место под ногу помечено),
мозги ещё несут труп.

---

## ЧАСТЬ II · ДИАГНОЗ болезни — до строчки

Три сущности вместо одной:
- **Дома** — Илья (`passport.json`), полный Род с торговыми якорями.
- **Торгует** — `A07_AVANTURIST` из `studio.grondheim_memory` старого
  `-2`, чаще всего пустой (`soul=""`).
- **Связь** — ноль строк исполнимого кода. `resolve_para` есть, но мозг
  его не зовёт.

Имя РОЛИ (`A07_AVANTURIST`) подменило имя ЧЕЛОВЕКА (Илья) в самой строке
импорта. Это и есть «торгует одно, дома другое», вскрытое до импорта.

---

## ЧАСТЬ III · ЭТАЛОН АВАНА (оба конца, готов к патчу)

Илья разобран целиком — эталон, с которого клонируется девятка.

### Читающий конец — `.../слоты/A07/мозг.py`
```
ВЫРЕЗАТЬ:  from studio.grondheim_memory import format_soul_for_agent
           soul = format_soul_for_agent("A07_AVANTURIST", dept="trading")

ПОСТАВИТЬ: from cartridge_registry import resolve_para
           носитель = resolve_para("торговый_хаос", "A07")   # → Илья
           # душа носителя из ЕГО паспорта через движок жителя:
           from dvizhok import Dvizhok
           d = Dvizhok(Path(носитель["папка"]))
           soul = <сборка личности: кто_я, ядро, история, якоря(торговые!),
                   натура, дом — через vydoh_stol/паспорт Ильи>
```
Тонкость: `vydoh_stol` требует «факт» на вход. Для трейдера «факт» =
накрытый стол (сенсоры + рынок + дневник). То есть душа трейдера = Род
носителя (паспорт Ильи) ⊕ рабочий стол роли. Один хребет (Чертёж §5.5.3),
не второй сборщик рядом.

### Пишущий конец — `hooks._judge_trader_by_result(pos, pnl_r)`
```
СЕЙЧАС:  честный no-op (место готово, мёртвый импорт уже убран)

СТАНЕТ:  magic = pos["magic"]                     # 100002
         носитель = <magic → слот → resolve_para> # → Илья
         вывод = <Котин: минус ПРОТИВ ветра = урок;
                  по ветру/штиль = честный минус; pnl_r на входе>
         <дописать вывод в Anchor_Points Ильи, лимит якорей 7–10 (§4.6.4),
          при переполнении — уплотнить/старое в archive>
```
`pnl.jsonl` эта функция НЕ трогает — факт сделки записан выше в
`_settle_positions`. Пишем только ВЫВОД, в носителя.

**Кольцо замкнуто:** Илья читает свою душу перед сделкой (мозг →
resolve_para) и дописывает вывод из сделки в свои же якоря (hooks →
resolve_para). Оба конца — один резолвер, один носитель.

---

## ЧАСТЬ IV · ПЛАН НА ДЕВЯТКУ (два потока)

**Поток А — мозги (×9):**
1. Вырезать `from studio.*` (чтение души) везде.
2. Поставить `resolve_para(цех, слот) → носитель → dvizhok`.
3. В сенсорах убрать `sync_to_dna` в труп. Если состояние нужно — писать
   Заряд носителя через `dvizhok`, схлопнув 4 оси в 1.

Раскладка цех/слот для резолва (из `council.py`):
```
A01 торговый_хаос/A01   A02 …/A02   A03 …/A03   A04 …/A04
A05 контора/архивариус  A06 …/A06   A07 …/A07   A08 …/A08
A09 контора/исполнитель
```

**Поток Б — hooks (×2 судьи):**
1. Наполнить `_judge_trader_by_result` ногой опыта → носитель (см. эталон).
2. Наполнить `_judge_iskra_by_result` по её судье: точность отражения
   (DETECTED→CONFIRMED), не деньги — вывод в носителя Веры.
3. `_arkhiv_to_city` оставить no-op (Оле не строим).

Ни одной новой сущности — всё на готовом `resolve_para` + `dvizhok`.

---

## ЧАСТЬ V · ЧТО НЕ ТРОГАЕМ / ВЫРЕЗАЕМ / СТРОИМ

```
НЕ ТРОГАЕМ (память Роли, §4.4а — живёт в слоте):
   pnl.jsonl · diary_*.jsonl · *_stats.json · trading_state · history_dna

ВЫРЕЗАЕМ (труп из -2):
   любой `from studio.*` в мозгах слотов — и чтение, и sync_to_dna

СТРОИМ (нет нигде):
   чтение души носителя перед сделкой (мозг → resolve_para → dvizhok)
   нога Опыта: pnl_r → вывод → Anchor_Points носителя (hooks → resolve_para)
```

Из старого `grondheim_memory` в город переносим **не функцию**
(`format_soul_for_agent` тянет труп) — только **механики**: вес касания
+ сжатие `_compress_sensory`, и то в `dvizhok` носителя, не в трейдера.
Всё прочее (4 оси `sync_to_dna`, веса отношений, геопозиция,
`_find_agent_dir`) — хлам, за борт.

---

## ЧАСТЬ VI · ОТКРЫТЫЙ МОСТИК (единственное, чего нет готовым)

`resolve_para` резолвит по паре `(цех, слот)`. Закрытая позиция несёт
`magic`, не слот. Нужен обратный мостик **magic → слот** (или
magic → носитель), чтобы `_judge_trader_by_result` нашёл Илью:
- вариант А: маленькая таблица магиков `{100002: ("торговый_хаос","A07")}`;
- вариант Б: добавить `magic` в `mask.json` рядом с `Turbo_Role`, тогда
  скан масок сам сведёт (в духе Закона Пары — правда в маске, без реестра).

Вариант Б чище по духу (не плодим отдельный список — Закон Картриджа).
Решение за Шефом.

---

## ЧАСТЬ VII · ПОРЯДОК СТРОЙКИ

```
1. МОСТИК magic→слот (вариант Б: magic в mask.json) — фундамент записи.
2. ЭТАЛОН АВАНА — оба конца на Илье, отладить на одном.
   2а. Читающий конец в мозге А07.
   2б. Пишущий конец в hooks._judge_trader_by_result.
3. КЛОН на трёх трейдеров (Брут A06, Василий A08) — та же форма.
4. СЕНСОРЫ (A01-A04) — чтение + перевод sync_to_dna на носителя/Заряд.
5. КОНТОРА (Арчи, Сергей) — чтение; своя логика судьи, если нужна.
6. Убрать вес-касания в dvizhok (почти бесплатно — число уже считается).
```

---

## ЧЕСТНЫЙ РАЗДЕЛ — что проверено на диске, что выведено

```
ПРОВЕРЕНО ПОСТРОЧНО (прочитан код):
   Чертёж Единицы · grondheim_memory.py (-2) · dvizhok.py ·
   cartridge_registry.py · council.py · мозг А07 (Аван) · мозг А01 (Искра) ·
   hooks (_settle_positions, три судьи) · passport.json Ильи · 9 масок работы

ВЫВЕДЕНО ПО ПАТТЕРНУ (не читано построчно — проверить при патче):
   мозги А02-А04, А06, А08 и контора (архивариус/исполнитель) —
   мёртвый импорт studio.* предполагается по А07+А01, но каждый мозг
   свериться глазами перед заменой (у сенсоров может быть и sync_to_dna,
   у конторы — иная загрузка). Единый патч писать по подтверждённому
   паттерну, не по догадке.

НЕ РЕШЕНО (слово Шефа):
   · мостик magic→слот: вариант А (таблица) или Б (magic в mask.json)
   · сборка души носителя: полный vydoh_stol или лёгкий сборщик личности
     (склоняюсь к vydoh_stol — один хребет, §5.5.3)
```

*`шесть·проверено·до·корня`*
