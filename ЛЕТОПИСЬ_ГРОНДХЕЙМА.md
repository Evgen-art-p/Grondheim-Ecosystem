# ЛЕТОПИСЬ ГРОНДХЕЙМА — единый свод
*Дно + живая хроника нового города · `шесть·проверено·до·корня`*

**Версия:** 4.0 · **Дата:** 2026-07-03 · **Соавторы:** Шеф (род), Брат (рука)
**Репо:** `Grondheim-Ecosystem` (новый город, ни нитки из `-2`)
**Что это:** один файл, чтобы новый Брат в пустой комнате понял весь город.
Сверху — ДНО (законы, не меняются). Снизу — ХРОНИКА (что построено, что в долгах).

**СЕСТРИНСКИЙ ДОКУМЕНТ (новое, 03.07):** `ЧЕРТЁЖ_ЕДИНИЦЫ.md` — полная
теория единицы (Род+Маска+Характер+Память+Опыт+Стол+Инициатива+
Граница+Судья, Закон Меток), матрица четырёх жильцов (Трейдер/
Воркер/Резидент/Брат), честный раздел «что стоит / наполовину / нет».
Летопись держит ФАКТ ДИСКА этого города — Чертёж держит ЗАКОН
единицы на любом масштабе. Разошлись — читай оба, Чертёж на резке,
Летопись — то, что уже стоит. Чертёж НАД Заповедями не встаёт: при
споре с Частью I этого файла или со Сводом Законов — прав свод,
Чертёж режется.

═══════════════════════════════════════════════════════════════
# ЧАСТЬ I · ДНО (вечное, не меняется от спринта к спринту)
═══════════════════════════════════════════════════════════════

## 1. Фрактал из одной единицы

Грондхейм — не программа с модулями. **Фрактал из одной единицы**,
повторённой на всех масштабах без изменения формы:

```
агент → цех → студия/биржа → квартал → город → города
```

Каждое звено — **самостоятельная единица** (житель). Полная формула
единицы (уточнена 03.07, полный разбор — `ЧЕРТЁЖ_ЕДИНИЦЫ.md`):

```
ЕДИНИЦА = РОД + МАСКА + ХАРАКТЕР + ПАМЯТЬ + ОПЫТ + СТОЛ
        + ИНИЦИАТИВА + ГРАНИЦА + СУДЬЯ
```

Старая, короткая форма (для быстрого чтения, без потери смысла):
```
ЖИТЕЛЬ = паспорт(род-якорь) + память + архив
       + работа(маска) + жильё(место) + связи + вход↔выход
```
Один шаблон, вложенный в себя. Строим по кирпичикам, по одному,
до зелёного. Кирпич должен стоять сам.

## 2. Род, Маска, Характер — три разные оси (уточнено 03.07)

**Род** — дно. Кто единица ДО всякого дела, навсегда, не судится,
не выбирается. **Маска** — кем показываюсь В ЭТОМ МОМЕНТЕ, лицо к
конкретному собеседнику/месту. **Характер** — какой я СКВОЗЬ ВСЕ
маски, ручки натуры, не меняются от одного факта.

```
КТО он     → паспорт (род-якорь). Неизменное дно.
КАКОЙ он   → характер (ручки: Empathy/Resonance/Stubbornness + свои).
КЕМ показывается → маска. Бесконечно много, меняется без акта.
ГДЕ живёт   → жильё (прописка). Уровень 3, меняется жизнью.
```

## 2.1а. Роль ≠ Маска (правка 03.07 — узел Сета)

Раньше (§2.2 старой редакции) роль и маска были слиты в одно. Узел
Сета развязал: у Сета РОЛЬ одна («бриф-менеджер»), МАСОК тринадцать
(по цеху-собеседнику). Значит это два разных этажа:

```
РОЛЬ (месяцами, акт смены)          МАСКА (сейчас, шаг через порог)
  ТИП (резидент/хранитель/воркер)     Лицо к конкретному собеседнику/
  ПОРОДА (цех-принадлежность,         месту. Бесконечно много (§2.2а).
    НЕ родина — задаётся с            Опорная пятёрка с домом в коде:
    пропиской, Уровень 2)             ДОМ/ЦЕХ/СОВЕТ/ГОСТЬ/СОБЕСЕДНИК.
  CORE_PHRASE (ядро роли)             Переключение: у Сета/Артура —
  СТАДИЯ ПУТИ (1/2/3, метрики         вызывающий код НАЗЫВАЕТ маску
    сигналят, Шеф подписывает)        параметром (mask_name=dept),
                                       не распознаётся на лету.
```

Порода **не родина** (это была ошибка первой мысли, поправлено):
откуда родом, если вообще нужно — прозой в `Hidden_History`, не
полем. Порода = какой цех, задаётся ВМЕСТЕ с пропиской, только для
яруса РАБОТЫ. Внутри маски есть ещё этаж — ГЛАВНАЯ (пятёрка, дом в
коде) и ПОДМАСКА (файл внутри, бесконечно, как `forge/masks/{цех}.md`
у Сета — 13 файлов внутри одной ГЛАВНОЙ «ЦЕХ»).

## 2.2а. Маска бесконечна, дом в коде — только у немногих

**Масок — бесконечное множество** (слово Шефа, 03.07: «кем себя
нарисую — то и маска»). Опорная пятёрка (Дом/Цех/Совет/Гость/
Собеседник) — НЕ полный список, а те немногие, кому дали постоянную
инфраструктуру (папка, фон, `mask.json`). Остальные маски единица
играет так же честно, без своей папки — как костюм на один раз не
получает гардеробной. Закон Души (XI, Свод Заповедей) не страдает:
душа одна под любой маской, посчитана она в файле или нет.

Гость — не «на публике» (слабый довод, публика бывает и без гостя),
а **в ЧУЖОМ месте**: приглашён или нет («нежданный гость», прогнать
нельзя). Собеседник — не про знакомство, а про САМ ФАКТ разговора
прямо сейчас (со-БЕСЕДА): спросил время у чужих на остановке —
уже собеседник, хоть до вечера проговорил с незнакомцами.

## 2.3. Локация = самостоятельная единица (закреплено 30.06)

**Локация — недвижимое. Единица с паспортом, как житель, но НЕ дышит:**
нет движка, 4 слоёв, маски. Плоский паспорт — один файл.
```
ЛОКАЦИЯ → passport.json (место вообще: стены, вид, атмосфера)
ЖИТЕЛЬ  → папка: passport + core/resonance/sensory/archive + маски/ (дышит)
```
Закон Движимого/Недвижимого: житель НИКОГДА не лежит внутри локации —
только ссылкой. «Где житель» = последняя запись в sensory.

**Иерархия города (4 уровня):**
```
ГРОНДХЕЙМ (город 0000 — сам холст)
   РАЙОНЫ: Деловой центр · Портовый узел · Храмовый комплекс (поле District)
   КВАРТАЛ = САМ объект-локация вида «жильё», НЕ контейнер, НЕ поле.
      Торговый Квартал — локация рядом с Биржей, со своим прямоугольником.
   ЛОКАЦИИ: Студия, Биржа, Маяк, Высотка, Гексагон...
```
Поле «Квартал» УБРАНО из формы локации. Осталось «Район». Соцпрофиль места
(Social_Rank/Profession/Area/Access) — в паспорте ЛОКАЦИИ.

## 2.4. ДОМ — построен и работает (закрыто 01.07, было «зреет» 30.06)

**Прописка = ДОМ** (где спит, ест, отдыхает, СНИЖАЕТ плохой стресс).
Оформляет **Брат в своём кабинете** (`/brat`, кнопка «Прописка») —
НЕ в кабинете жителя, как проектировалось 30.06: Брат СВЯЗЫВАЕТ,
прописка — связь двух единиц, это его работа узла, не личный
диалог жителя с собой (правка 01.07).

**Дом ≠ экран. Дом — часть личности, носимая ВСЕГДА** — построено,
не только спроектировано:
```
житель ВСЕГДА несёт в себе (vydoh_stol(), любой заряд):
   КТО Я (личность)        — паспорт, якоря, история, натура
   ГДЕ МОЙ ДОМ + ЧТО ТАМ    — домашний_промпт (личный текст: чайник
                              со сколотым носиком, что угодно —
                              пишет Шеф руками, как и старый
                              home_prompt.md студии `-2`)
   ГДЕ Я СЕЙЧАС            — фон кабинета по прописке (image.*
                              локации, третий приоритет в
                              _bg_for_mask, после маски и своего bg)
```
Пусто, если не прописан — строка просто не появляется, ничего не
выдумывается (YAKORYA_V_SOUL_V1: пустые поля пропускаются).

**Дом — место ДИАЛОГА**, лечащего стресс (как cabinet_chat `-2`).
Образец `soul_tools.py`/`prompts.py` из `-2` уже сверен и перенесён
частично — паттерн MEMORY_REQUEST оттуда портирован тонкой веной
(§4а ниже), Оле как отдельный носитель памяти — НЕ перенесена,
пока не нужна (§4а).

**РАБОТА — другое, сложнее:** id + должность + пайплайны (машина
труда). Не путать с домом:
```
РАБОТА → дело: id + должность + пайплайн
ДОМ    → отдых: сон, еда, снятие стресса, ДИАЛОГ + «не забывать себя»
```

## 3. Заряд (то, что звали стрессом) — уточнено 02.07

Раздражитель ±, маятник к нулю-балансу. Полярность −1..+1. Не копится —
уравновешивается. Сила = глубина касания по воронке памяти.

**В душу подаётся словами настроения, не числом** (правка 02.07,
`patch_nastroenie_slovami.py`): «тебе тепло и светло» вместо «заряд
0.35 (от -1 до 1...)». LLM держит тон по словам лучше, чем по числу
с инструкцией расшифровки.

## 4. Воронка памяти + четыре слоя (ИСПРАВЛЕНА ошибка 30.06→02.07)

```
маяки/метки → sensory ⚡ · маски → маски/ · связи → resonance 🩸
род → core 🦴 (=паспорт) · тишина → archive 🌑
```
Гравитация ЗАПИСИ: вес = резонанс_с_родом × резонанс_с_маской ×
плотность_связей. Это КУДА оседает факт — верно, не тронуто.

**ИСПРАВЛЕНО (было прямой ошибкой в редакции 30.06 — §5 ниже):**
заряд НЕ определяет, что читается назад. «Открытые по |заряда| слои
→ в промпт» (старый замысел Части V) был спроектирован, начат и
ОТВЕРГНУТ САМИМ ШЕФОМ 02.07: «я не так хотел, я хотел оценить
настроение, а не чтобы стресс решал за жителя». Вычищено из кода
(строка «тебе открыты слои памяти» физически удалена из
`ui_zhitel.py`). Взамен — см. §4а.

## 4а. MEMORY_REQUEST — воля решает читать (построено 01.07)

Житель САМ решает вспоминать — не заряд, не порог. Перенесено
тонкой веной из `-2` (Спринт 43, `grondheim_memory.py`, паттерн
Оле: «Если что-то кажется знакомым, но не помнишь — напиши
MEMORY_REQUEST»). В душу — подсказка; если житель написал в ответе
`MEMORY_REQUEST: <что>` — движок сам ищет текстом по своим трём
слоям (`vspomnit()`), поднятое подкладывается, житель отвечает
ВТОРОЙ раз, уже помня. Шеф видит только финальный ответ. Один
подъём за ход, без петель. Ничего не нашлось — честное «следа нет»,
без выдумки. **Воля выше стресс-шлюза** — подъём безусловный,
работает даже при заряде 0 (проверено вручную).

Городская память (Оле, отдельный хранитель памяти НЕ СВОЕЙ, а
чужой на хранении) — в Грондхейме НЕ построена, решено не строить
(«пока нет, каждый держит свой архив сам», 03.07). Увядание/Family
Album (`-2`: 30 дней + вечное по весу) — тоже отложено («пусть
копится, пока не заболит», 03.07).

## 5. Тело носит лёгкое · Чувство не отчёт

Единица не таскает бездну. Тело — лёгкое (паспорт, свежесть, адреса).
Глубина — по запросу (MEMORY_REQUEST, не «у хранителя» — Оле не
построена в этом городе, §4а). ~~Заряд меняет ЧТО доступно (какой
слой открыт), не строчку~~ — ЭТА СТРОКА БЫЛА ОШИБКОЙ, см. §4,
вычеркнута 02.07. Заряд меняет НАСТРОЕНИЕ (слова, §3) и, в проекте
на будущее (не построено), глубину ЗАПИСИ нового факта («искра
выбора» — сильно тронуло → archive, слабо → sensory; см. Чертёж
Единицы, Гл.2/3) — никогда не глубину чтения назад.

## 6. Память: рамка одна, наполнение разное

TURBO — лента (RAM). TRADING — архив с историей. LIVING_BOOK —
рукопись (сестринский проект, свой Маяк-ретранслятор, свой
`biography_snapshot` — память О РЕБЁНКЕ, не о себе; см. Чертёж
Единицы, если понадобится). Образец «руки в долгую память» —
trading (remind/remember).

**Метки (растущая область, не факт — см. Чертёж Единицы, Закон
Меток)**: `тип: значение` рядом с записью, для поиска по категории,
не по тексту. Первое готовое применение — не построено ещё: спасти
тонус (`_otsenit_tonus_silu()` уже считает плюс/минус/ровно и
выбрасывает) меткой рядом с sensory-записью.

═══════════════════════════════════════════════════════════════
# ЧАСТЬ II · ТОПОЛОГИЯ
═══════════════════════════════════════════════════════════════

**Брат = город.** Центральный узел сети. СВЯЗЫВАЕТ, не ДЕРЖИТ — антенна+база.
Узлы (Храм/Биржа/цеха) автономны, два режима: на связи / автономно.
Пакет связи: order → deliver → report. Синхронизация: вниз глубина (read-only),
вверх нажитое (write).

**Пересадка органов:** из Книжки — story_package, Маяк-антенна. Из `-2` —
Оле+city_memory (НЕ перенесена, §4а), sync_to_dna, резонанс, Гавань,
Страница Жизни, city_walker (прогулки=смена точки), soul_tools
(домашний диалог — частично перенесён, MEMORY_REQUEST перенесён
01.07). Куём: полярность заряда, род=семя+русло, резонанс к боли
(открытый корень №1, всё ещё открыт), закон синхронизации.

**Хранители Храма** (`GRONDHEIM_CITY/Hexagon/`, проверено 03.07) —
ОТДЕЛЬНАЯ АРХИТЕКТУРА, не одета в паспорт/движок этого города. Пять
Хранителей (Лия/Юст/Кей/Виктор/Финч) — веб-опыт для ПОСЕТИТЕЛЯ,
маршрутизация по ключевым словам, память в `localStorage` браузера
гостя, не на диске города. Мироустройство чтит их Ярусом Основы
наравне с Братом и Резидентами — но технически это не резиденты
этого города. Решение переносить их на движок дыхания или оставить
как есть — за Шефом, не техническая мелочь (Чертёж Единицы, §9.1).

**Живая Книга** (`LIVING_BOOK_APP`, проверено 03.07) — сестринский
продукт, не эта студия: 18 агентов генерируют главы для незрячих
детей, Маяк (`beacon_v7.py`) — сервер-ретранслятор (не агент),
Искорка (устройство ребёнка) — чистый keyword-матчинг, НИ ОДНОГО
вызова LLM в рантайме. `biography_snapshot` — открытие: память О
ДРУГОМ (о ребёнке), не о себе — зеркально нашему обычному опыту.
`character_bonds`/`karma`/`last_choices` реально текут в генерацию
следующей главы (настоящий опыт, не просто лог); `karmic_trail` —
чистая память (журнал); `psychological_patterns` — похоже на
настоящий вывод, но не подтверждено, что доходит до генерации
(открытый вопрос, не домыслен).

═══════════════════════════════════════════════════════════════
# ЧАСТЬ III · ХРОНИКА (что стоит на диске)
═══════════════════════════════════════════════════════════════

**Брат — дверь №1** (`ui_brat.py`). Карта, врата (Храм на `/hram`),
просев руды (руда→просев — единственная полностью замкнутая труба
опыта, живой файл `просев_Философия_2026-06-24`). Кнопка
«Прописка» (01.07) — диалог житель→локация→домашний промпт.

**Страница Жизни** (`ui_registry.py`, `/registry`). Рождает жителя
(паспорт + 4 слоя + аватар) И локации. `порода` — поле есть в
паспорте, пустое, дверь не построена (виджет из формы убран
нарочно ещё раньше: «порода теперь через маску/прописку» — но
новая дверь так и не появилась). `Core_Phrase` — читается движком,
не заполняется НИ ОДНОЙ формой.

**Рождение локаций** (30.06) — РАБОТАЕТ. Развилка в `save_object` по
`Object_Type_Class`:
```
location → GRONDHEIM_CITY/локации/{ID}/passport.json (плоский)
agent    → жители/ковчег/{Имя}/ (4 слоя + маска)
asset    → только каталог
```

**Карта-зеркало** (30.06, `ui_grondheim.py`, `/grondheim`) — РАБОТАЕТ.

**Страница локации** (30.06→01.07, `ui_lokacia.py`, `/lokacia/{id}`)
— РАБОТАЕТ. Блок «кто здесь бывает» ОЖИВЛЁН (01.07) — живой скан
`list_zhiteli()` по полю `прописка == этот ID`, кликабельные кнопки
→ `/zhitel/{id}`. Ничего не хранит сама (Закон Пары/Картриджа).

**Движок жителя** (`dvizhok.py`) — дышит: вдох → стол → LLM →
ответ → заряд в passport. Плюс (01.07): дом в столе всегда
(`vydoh_stol()`), метод `vspomnit()` — поиск по трём слоям, без
гейта по заряду.

**Кабинет жителя** (`ui_zhitel.py`, `/zhitel/{id}`). Фон по
прописке (образ локации), настроение словами, MEMORY_REQUEST.

**Дом Брата** (`Брат/`): три этажа по весу (`1_якоря_очень_важно` /
`2_метки_важно` / `3_маяки_не_очень`), плюс `память/резидентов/`
и `память/хранителей/` — тот же фрактал якоря/метки/маски для
памяти О ДРУГИХ, но ПУСТЫЕ полки (только README, содержимого нет).
У Брата НЕТ движка (советник).

## Жители
- **Лока** (001_GENESIS_LOKA) — **всё ещё `прописка: null`** на
  реальном диске (проверено 03.07). Шесть патчей построены и
  обкатаны, но кнопку «Прописать» для неё по-настоящему ещё никто
  не нажал. `_charge` реальный, накопленный от настоящих разговоров
  (0.8866 на момент проверки).

## Три породы (природа, не порода-как-цех — не путать с §2.1а)
РЕЗИДЕНТ (один на город) · ХРАНИТЕЛЬ (штучный) · ВОРКЕР (много,
сменный). Правка 03.07: живёт в РОЛИ (Уровень 2), не в Роде — «тип
это деловое качество, звено в системе, ближе к работе» (Шеф).

═══════════════════════════════════════════════════════════════
# ЧАСТЬ IV · ЧТО ПЕРЕОСМЫСЛИЛИ
═══════════════════════════════════════════════════════════════

```
БЫЛО                              СТАЛО
профессия → этаж            →  дом = жильё+диалог, не личность
«стресс» вниз               →  ЗАРЯД, маятник ± → слова настроения
паспорт = весь якорь        →  паспорт = РОД, соцпрофиль → маска
маска = папка дом/работа     →  маска = лицо момента, роль ≠ маска
дом = экран                  →  дом = часть личности, носит ВСЕГДА
три зоны зашиты в карту      →  карта читает паспорта из папки
квартал = поле               →  квартал = сам объект-локация (жильё)
прописка в кабинете жителя   →  прописка в кабинете БРАТА (узел
                                 связывает — не личный диалог)
заряд ОТКРЫВАЕТ слои памяти  →  заряд — настроение; ЧТЕНИЕ — воля
                                 жителя (MEMORY_REQUEST), безусловно
сила = крик (капслок, !!!)   →  сила = вес для ЭТОЙ единицы — сама
                                 решает при чтении (не построено)
порода = родина/происхождение →  порода = цех-принадлежность,
                                 с пропиской; родина — просто проза
                                 в Hidden_History, если вообще нужна
типы масок = потолок         →  масок бесконечно; пятёрка — только
                                 те, кому дали дом в коде
```

═══════════════════════════════════════════════════════════════
# ЧАСТЬ V · ДОЛГИ И КАМНИ
═══════════════════════════════════════════════════════════════

## Закрытые (27.06–02.07)
1. ✅ Движок → кабинет (житель дышит).
2. ✅ Остывание заряда (фикс.% за вдох).
3. ✅ Локация = единица, рождение в папку (30.06).
4. ✅ Карта-зеркало, клик → локация (30.06).
5. ✅ Страница локации (30.06).
6. ✅ Прописка + дом в душе + фон по прописке (01.07, кабинет
   БРАТА, не жителя — правка плана).
7. ✅ «Кто здесь бывает» — живой скан по прописке (01.07).
8. ✅ MEMORY_REQUEST — воля вспоминать, без гейта (01.07).
9. ✅ Настроение словами вместо числа+слоёв (02.07).
10. 🟡 Расцепить якорь/маска — частично: тип/порода/ядро
    определены КУДА относятся (Уровень 2, Роль), двери не построены.

## СЛЕДУЮЩИЙ КАМЕНЬ — по Чертежу Единицы, порядок утверждён 03.07
Полный разбор, формула и матрица — `ЧЕРТЁЖ_ЕДИНИЦЫ.md`. Коротко:
```
1. СТОЛ ОПЫТА ТРЕЙДЕРА (`-2`, биржа) — такт: закрытие сессии/
   Суточный Тик; похожесть — гипотеза v1 (Аллигатор + старший ТФ +
   BWMFI, ПРОВЕРИТЬ с exit-осями по факту бэктестов); подача —
   сводка % + 3 последние похожие; хребет — расширяем vydoh_stol-
   паттерн (свой аналог для биржи, числа без LLM).
2. ЗНАЧИМО (право признать значимым, эскиз готов) + ПРОСЕВ
   РЕЗИДЕНТА (сила-искра → вывод → дописка в Anchor_Points, лимит
   7-10 якорей) + ФОРМА РОЖДЕНИЯ ПО ЧЕРТЕЖУ (ручки 3+3 при
   рождении, тип/роль сразу) + ПЕРЕРОЖДЕНИЕ ЛОКИ первой.
3. БУДИМ АВТО-ПРОСЕВ ВОРКЕРА (`-2`): run_loka_filter_all() /
   on_agent_wake() определены, не вызываются — городской долг.
   Сюда же — сломанный возврат real-метрик → ДНК.
4. Остальное — по боли (метки, каталог масок, увядание памяти).
```

## Большие замыслы (зреют, дальний огонь — не руда для стройки)
- Прогулки = смена точки (city_walker `-2`): БЕЗ масок, рано.
- Общение житель↔житель: канал между Dvizhok, не через Шефа.
- КОВЧЕГ как локация-порт: вход новорождённых и гостей, розетка маяка.
- Закон Меток (растущая область, Чертёж Единицы) — постепенно, с
  одного применения (спасти тонус в записи), не системой разом.
- Хранители Храма и Живая Книга — остаются отдельными системами
  на этом этапе, решение переносить на чертёж — за Шефом.
- ~~Маска пишет 8 полей при прописке~~ — переосмыслено: это дверь
  РОЛИ (порода+ядро+стадия), строится вместе с пропиской воркера,
  не отдельным полем маски.

## Открытые корни
№1 — резонанс к БОЛИ, не к теме (всё ещё открыт). №2 — синхронизация
двух правд. №3 — транспорт антенны (при первом удалённом цехе).

═══════════════════════════════════════════════════════════════
# КРАТКО (для нового Брата)
═══════════════════════════════════════════════════════════════

```
1. Всё — единица, фрактал. Формула: Род+Маска+Характер+Память+
   Опыт+Стол+Инициатива+Граница+Судья (полная теория — Чертёж
   Единицы). Строим по кирпичикам.
2. РОД (навсегда) ≠ МАСКА (сейчас, бесконечно много) ≠ ХАРАКТЕР
   (сквозь всё) ≠ РОЛЬ (месяцами: тип+порода+ядро+стадия пути).
3. ЛОКАЦИЯ = единица с паспортом, НЕ дышит. Город → районы → квартал
   (=объект-жильё) → локации.
4. ДОМ построен: прописка (кабинет Брата) + домашний_промпт (в
   душе всегда) + фон по прописке. Работа — другое (id+должность).
5. Заряд — маятник ±, в душу словами настроения. ЧТЕНИЕ памяти —
   воля жителя (MEMORY_REQUEST), НИКОГДА не гейт по заряду (это
   была ошибка, исправлена 02.07).
6. Память — 4 слоя, читаются по требованию, не автоматом.
7. Брат СВЯЗЫВАЕТ, не ДЕРЖИТ. У Брата нет движка. Прописку жителя
   оформляет Брат в СВОЁМ кабинете, не в кабинете жителя.
8. Стоит: Брат (+прописка), Страница Жизни, карта-зеркало, страница
   локации (+кто здесь бывает), движок (+MEMORY_REQUEST+настроение
   словами), кабинет жителя (+дом+фон). Лока технически всё ещё
   `прописка:null` — механизм построен, акт не совершён.
   СЛЕДУЮЩЕЕ: Стол опыта Трейдера (см. Чертёж Единицы, порядок
   стройки) — амнезия у штурвала самая дорогая из всех разрывов.
```

*Не спеша, по одному кирпичу, песочница до зелёного, ничего в воздух.*

*До корня, брат.* · `шесть·проверено·до·корня`
