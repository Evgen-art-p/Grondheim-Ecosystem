# Маячки — где встали
*Поверхность · ситуативное · гаснет, когда сделано*

---

Это верх. Маячки момента. «Тут остановились», «это открыто»,
«сюда вернуться». Гаснут, когда сделано. В свежей сессии Брат
открывает их первыми — чтобы встать на нить, а не искать её.

---

## 🟢 Сделано
- Папка меток Брата заложена. Три этажа. Подпись на дне.
  (живёт в GRONDHEIM_CITY/Hexagon/Брат/ — канон Шефа,
   «не резидент и не Хранитель, якорь под масками»)
- Движок Брата развёрнут начисто: тело + маска + руда +
  печать. Без резидентской машины (format_soul/sync_to_dna
  Брату НЕ нужны — у Брата свой закон).
- Карта слоёв важности Брата снята по ЖИВОЙ репе (см. ниже).

## 🗺 КАРТА СЛОЁВ ВАЖНОСТИ БРАТА (по живой репе)
Принцип Шефа: вес = близость к смыслу. Не плоская сеть —
градиент. Близкое тяжелее далёкого. По такому — ВЕСЬ город.
Важность определяется РОЛЬЮ агента, не цехом, где он сидит
(Архивариус сидит в trading/A05, но он слой 1, не слой 3).

  СЛОЙ 1 — архиваторы памяти (тяжелейший):
    • Оле        — studio/memory_tools.py (remember/remind/release/decline)
    • Финч       — studio/garden_tools.py (сад, потенциал)
    • Архивариус — studio/modules/trading/A05 · arkhiv_live.py
  СЛОЙ 2 — резиденты (жители города, выше финализаторов):
    • studio/modules/residents/ (Лока, Джем, …)
  СЛОЙ 3 — финализаторы цехов (последний на выходе):
    • находить ПРОГРАММНО: manifest.json цеха → "qa_agent"
    • НЕ списком руками (протухнет на новом цехе — баг #35)
    • образец: trading/manifest.json → qa_agent = A09 Исполнитель
  СЛОЙ 4 и ниже — рабочие руки цехов (сенсоры, трейдеры …):
    • чем дальше от памяти/жизни, чем ближе к голому
      инструменту — тем легче для Брата.

## 🔆 Открыто — следующая сессия (по порядку)
- **ЭТАП СЕЙЧАС: полочки (раскладывать).** Научить Брата
  правильно по полочкам складывать входящую информацию —
  по уровням и граням. ИСПОЛЬЗОВАТЬ (руки к памяти, выдача
  по запросу) — ЭТАП ПОСЛЕ. Не лезть в «использовать» раньше
  времени: сперва склад, потом применение.
- **Подключить Брата к слою 1 (3 архиватора).** Образец —
  рука берущая Архивариуса: from studio.memory_tools import
  remind; remind(query, top_k). В try/except — никогда не
  роняет. Запрос человеческим текстом (Гавань ищет по смыслу).
- **Градиент весов по слоям** — Брат читает manifest.json
  каждого цеха, строит карту важности сам, не хардкодом.
- **Грани сортировки полочек** — Шеф поможет назвать оси
  (смысл/замысел/принадлежность/важность — добить набор:
  полный или открытый).

## ⚙️ Долги города (не потерять)
- Loka-фильтр спит: run_loka_filter_all / on_agent_wake
  в grondheim_memory.py определены, не зовутся нигде.
  Привязать к ночному циклу/тику.
- Висячая нитка Финча: garden_tools зовёт write_city_memory —
  такой функции НЕТ (правильная — remember). Падает в try/except.
- Карта _find_agent_dir прибита к studio/modules — Брата
  из Hexagon она не видит (и не должна: Брат к памяти города
  стыкуется через memory_tools, не через резидентскую машину).

---
*Маячок горит — иди на свет. Сделал — гаси.*
*`шесть·проверено·до·корня`*
