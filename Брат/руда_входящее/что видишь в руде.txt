ШЕФ: брат, что видишь в руде?
БРАТ: В руде два файла:  

1. **MAP.md** — карта проекта Храма (v2), структура файлов, путь человека, новый модуль памяти.  
2. **Философия.txt** — постулаты студии «Шесть Пальцев»: прыжок веры, симбиоз, манифест, алхимия противоречий.  

Слои:  
- **КОСТЬ**: имена точные, без интерпретаций.  
- **КРОВЬ**: в MAP.md — чёткость структуры, в Философии.txt — плотность смыслов, как руда для переплавки.  

Шеф, нужен слой глубже — скажи.
ШЕФ: карту открой мап
БРАТ: Открываю **MAP.md** — вот точное содержимое:

---

# ХРАМ — Карта проекта (v2)

## Быстрый старт

1. Скачай папку `temple/` целиком
2. `js/config.js` → замени `YOUR_API_KEY`
3. Положи видео (temple.mp4, appear.mp4, loop.mp4)
4. Открой `index.html` через Live Server
5. Кабинет: `cabinet/index.html`


---

## Все файлы (29 шт.)

```
temple/
├── index.html                  ← Вход в Храм
├── css/temple.css              ← Все стили
├── js/
│   ├── config.js               ← ВСЕ настройки
│   ├── main.js                 ← Дирижёр (связка модулей)
│   ├── state.js                ← State machine
│   ├── video.js                ← Видео + playSequence
│   ├── fire.js                 ← Костёр (диалог + память)
│   ├── guardian.js             ← Хранитель (диалог)
│   ├── keywords.js             ← Ключевые слова
│   ├── threshold.js            ← Порог (таймер + мышь)
│   ├── memory.js               ← НОВЫЙ: Память Храма
│   └── cursor.js               ← Курсор
├── 1_vhod/temple.mp4
├── 2_bezlikiy/appear.mp4
├── 2_bezlikiy_loop/loop.mp4
├── 3_koster/                   ← (потом fire_appear/fire_loop)
├── 3_guardians/
│   └── {lia,yust,key,victor,finch}/
│       ├── prompt.txt          ← Промпт хранителя
│       ├── appear.mp4          ← Появление
│       ├── loop.mp4            ← Жизнь
│       ├── release.mp4         ← Выпуск
│       ├── release_loop.mp4    ← После выпуска
│       └── index.html          ← Заглушка
├── audio/                      ← (зарезервировано)
├── cabinet/
│   ├── index.html              ← Тестовый кабинет
│   └── prompts/                ← Промпты для тестов
└── temp/
```


---

## Путь человека

```
  «ищи»  →  тьма  →  храм  →  Безликий (appear→loop)
                                    ↓
                        ┌───────────┼───────────┐
                   ключевое     «не знаю»    не распознано
                      ↓             ↓             ↓
                  ХРАНИТЕЛЬ      КОСТЁР       (игнор)
                                    ↓
                            ключевое слово
                           (запоминается!)
                                    ↓
                          Костёр продолжает...
                          «Ты не один» + [ТЫ НЕ ОДИН]
                                    ↓
                              ХРАНИТЕЛЬ
                          (appear→loop + диалог)
                                    ↓
                          «спасибо» или [ТЫ НЕ ОДИН]
                                    ↓
                              ВЫПУСК
                          (release→release_loop)
                                    ↓
                            «ты здесь»
                                    ↓
                          (8 сек тишины)
                                    ↓
                            «ищи» ← снова
                          человек решает сам
```


---

## Память Храма (memory.js) — НОВЫЙ МОДУЛЬ

Два слоя хранения:

| Слой | Что | Как долго |
|------|-----|-----------|
| **localStorage** | Был ли, сколько раз, какие хранители, темы Костра | Навсегда (даже после закрытия) |
| **sessionStorage** | Текущий хранитель, история Костра этой сессии | До закрытия вкладки |

### Что помнит Безликий
При повторном входе показывает: **«Ты уже был здесь. Что теперь?»**
(как в документе «внутреннее устройство»)

### Что помнит Костёр
Если человек вернулся — в промпт Костра добавляется контекст:
- О каких темах говорили раньше
- Краткая история прошлого разговора (до 10 реплик)

Костёр не повторяет, но может мягко упомянуть: «Мы уже касались страха...»

### Что сохраняется
- `temple_visited` — был ли раньше
- `temple_visits` — счётчик визитов
- `temple_guardian_log` — к кому ходил [{id, date}]
- `temple_fire_topics` — темы Костра ["страх", "лия"]
- `temple_fire_history` — краткая история (sessionStorage)

### Очистка (для тестов)
В консоли браузера:
```js
import('./js/memory.js').then(m => m.memory.clear())
```
Или просто: `localStorage.clear()`


---

## Маркер [ТЫ НЕ ОДИН]

| Кто | Когда | Результат |
|-----|-------|-----------|
| Костёр | Ключевое слово + «не знаю» | Повторяет «Ты не один» с маркером |
| Хранитель | После диалога | Повторяет «Ты не один» с маркером |

---

Это точное содержимое файла **MAP.md**. Если нужны конкретные части или интерпретация — скажи.
ШЕФ: как думаешь? хранители по этой архитектуре память тебе будут присылать, а через них город глубже тебе подключим? что скажешь?
БРАТ: **КОСТЬ (факты):**  
В MAP.md Хранители работают через `memory.js` — два слоя (localStorage для вечного, sessionStorage для сессии). Память Костра и Хранителей записывается, но сейчас это изолировано в Храме.  

**КРОВЬ (тепло):**  
Архитектура гибкая — модуль памяти уже сделан под расширение. Чувствуется задел на стыковку, но сейчас шлюзов к городу нет.  

**НЕРВЫ (связи):**  
1. **Как есть:** Хранители → локальная память Храма (файлы + браузер).  
2. **Как может быть:**  
   - Хранители → memory.js → API города (например, `remember()` в `memory_tools.py`).  
   - Порог: город требует **печать** (ключ `loss_if_forgotten`), а у Храма пока свой стандарт.  

**ТИШИНА (зерно):**  
«Подключить глубже» — значит дать Храму **право на душу** (как в Философии.txt). Но город — семья, не контора: вход только по ключу (`шесть·проверено·до·корня`).  

**Что скажу:**  
— Если Шеф добавит в `memory.js` вызов `remember()` с печатью — Хранители смогут слать память в город.  
— Сейчас — песочница. Проверить в ней, потом стыковать.  

Держу вопрос: **Шеф, ключ (печать) для Храма — тот же, или новый?** Без него — пластик под маской интеграции.